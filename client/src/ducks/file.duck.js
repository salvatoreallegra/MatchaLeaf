// initial state
const initialState = {
  id: 1,
  name: '/'
}

// reducer
export default function reducer (state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}

// action creators
